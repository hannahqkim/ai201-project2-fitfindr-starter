"""Pytest configuration.

This file's presence at the project root puts the root on sys.path, so tests
under tests/ can `import tools` and `from utils.data_loader import ...` without
any extra path juggling. Run the suite with `pytest tests/` from the root.
"""
